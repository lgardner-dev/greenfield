# Fonts

Place a `.ttf` or `.otf` font here for local sandbox text rendering.

The sandbox first looks for `assets/fonts/Greenfield-Regular.ttf`. If that file is not present, it falls back to a common system font path on developer machines.
